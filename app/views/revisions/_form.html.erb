<%= simple_form_for(@revision, url: game_revisions_path(@game)) do |f| %>
  <% if @revision.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= "#{pluralize(@revision.errors.count, "error")} prohibited this post from being saved:" %>
      </h2>
      <ul>
        <% @revision.errors.full_messages.each do |msg| %>
          <li>
            <%= msg %>
          </li>
          <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.input :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.input :description, :as => :ckeditor, input_html: {:ckeditor => {:toolbar => 'FULL'}}, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.button :submit %>
  </div>
<% end %>
